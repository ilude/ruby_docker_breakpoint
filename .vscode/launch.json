{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Docker",
            "type": "Ruby",
            "request": "attach",
            "remotePort": "1234",
            "remoteHost": "127.0.0.1",
            "remoteWorkspaceRoot": "/app",
            "cwd": "${workspaceRoot}",
            "showDebuggerOutput": true
        }
    ]
}
